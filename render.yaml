services:
  - type: web
    name: ecomops-mobile-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PYTHON_VERSION
